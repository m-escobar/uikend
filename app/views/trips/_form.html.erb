<%= simple_form_for(@trip) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :name, label: "Nome do pacote:" %>
  </div>

   <div class="form-inputs">
    <%= f.input :description, label: "Descrição do pacote:" %>
  </div>

   <div class="form-inputs">
    <%= f.input :place, label: "Local:", input_html: { type: "search", id: "address", placeholder: "" } %>

    <p>Selecionado: <strong id="address-value"></strong></p>
    <script src="https://cdn.jsdelivr.net/npm/places.js@1.16.4"></script>
    <script>
    (function() {
      var placesAutocomplete = places({
        appId: "plDNP2V5J54H",
        apiKey: "4e4a09d70e0b7682eceeadfa62eb2221",
        container: document.querySelector('#address')
      });
      console.log(placesAutocomplete)
      var $address = document.querySelector('#address-value')
      placesAutocomplete.on('change', function(e) {
        $address.textContent = e.suggestion.value
      });

      placesAutocomplete.on('clear', function() {
        $address.textContent = '';
      });

    })();
    </script>
  </div>

   <div class="form-inputs">
    <%= f.input :capacity, label: "Capacidade do pacote:" %>
  </div>

  <div class="form-inputs">
    <%= f.input :hotel, label: "Nome do hotel:" %>
  </div>

  <div class="form-inputs">
    <%= f.input :schedule, label: "Roteiro:" %>
  </div>

  <div class="form-inputs">
    <%= f.input :price, label: "Preço:", placeholder: 'Colocar o preço sem centavos' %>
  </div>

  <div class="form-inputs">
    <%= f.input :start, label: "Começa em:" %>
  </div>

  <div class="form-inputs">
    <%= f.input :end, label: "Termina em:" %>
  </div>

  <div class="form-inputs">
    <%= f.input :photo %>
    <%= f.input :photo_cache, as: :hidden %>
  </div>

  <div class="form-actions btn btn-success mb-2 p-0">
    <%= f.button :submit %>
  </div>
<% end %>
